# Evaluation contract

Fixed command, Python 3.12 lock, one `.venv`; Hugging Face `cpu-upgrade`. Estimate 1 core, under 1 GiB, under 2 minutes. Accept only if all universal proof obligations and the independent DAG checker pass and disabling witness protection exits nonzero.
