# Limitations and deviations

This is a machine-checked reconstruction of the quantified contradiction, not a proof-assistant kernel. Its trusted arithmetic facts are that `n+1` distinct points from `h_n` include a negative, `T=max(d,m)` reaches either finite threshold, and `{1,...,d}` is already exhausted. No threshold is bounded or sampled.
