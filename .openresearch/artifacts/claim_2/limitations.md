# Limitations and deviations

Lean kernel-checks the exact all-integer support intersection and exhaustion contradiction for arbitrary `d`. The standard-side pigeonhole step and composition at `T=max(d,m)` remain source-audited. No threshold is bounded or sampled.
