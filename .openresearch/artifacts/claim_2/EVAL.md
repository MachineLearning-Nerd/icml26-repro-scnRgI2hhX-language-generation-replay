# Evaluation contract

Use the fixed repository command on Hugging Face `cpu-upgrade`; estimate 1 core, under 1 GiB, under 2 minutes. Accept only when the arbitrary-threshold certificate and independent checker pass and disabling replay legality causes a nonzero exit.
