# Evaluation contract

Fixed locked command on Hugging Face `cpu-upgrade`; estimate 1 core, under 1 GiB, under 2 minutes. Accept only if the full arbitrary-phase proof DAG and independent checker pass and removing the replay permission for `star^z` exits nonzero.
